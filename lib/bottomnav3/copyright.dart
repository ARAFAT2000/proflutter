import 'package:flutter/material.dart';

class Copyrihgt extends StatelessWidget {
  const Copyrihgt({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.cyanAccent,
      appBar: AppBar(
        title: Text('Copy'),
      ),
    );
  }
}

